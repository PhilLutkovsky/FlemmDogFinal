
class Cart{
  constructor(i,q){
  this.itemList = i;
  this.itemQuantity = q;
  }

  addItem(i,q){
    itemList.push(i);
    itemQuantity.push(q);
  }

  totalCart(){
    var total = 0;

    for(t=0; t<this.itemList.length; t++);
      total += (this.itemList[t]*this.q[t]);
  }
    return total;

  subCart(days){
    var subCart = new Cart();

    for(t=0; t<list.itemList.length; t++){
      if(this.itemList.shipping == )
    }



  }
}
